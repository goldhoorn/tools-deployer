# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(deployerTaskLib)
ADD_LIBRARY(${DEPLOYER_TASKLIB_NAME} SHARED 
    ${DEPLOYER_TASKLIB_SOURCES})
add_dependencies(${DEPLOYER_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${DEPLOYER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${DEPLOYER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${DEPLOYER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${DEPLOYER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${DEPLOYER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${DEPLOYER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${DEPLOYER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${DEPLOYER_TASKLIB_HEADERS}
    DESTINATION include/orocos/deployer)

